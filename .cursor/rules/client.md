# Client rules

- Use existing component if possible, even if you have to adapt them a bit
- Always double check if your new code can be refactored with existing code
- Don't forget about the shared code in `/shared` directory for API call and common